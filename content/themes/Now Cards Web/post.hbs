{{!< default}}

<main>
    <div class="post-logo">
        {{#if @blog.logo}}
            <a href="{{@blog.url}}"><img src="{{@blog.logo}}" alt="Blog Logo"></a>
        {{else}}
            <h1><a href="{{@blog.url}}">{{@blog.title}}</a></h1></a>
        {{/if}}
    </div>

        {{#post}}

    <article>
        <div class ="post-header">
            <h1>{{{title}}}</h1>
            <span> {{#if tags}}{{tags separator=" | "}}{{/if}}</span> <!-- 'tags' que aparecen debajo del titulo del post -->
        </div>

        <section>
            {{content}}
        </section>

        <footer class="post-footer">

            <!-- Sección con los datos del autor -->
            {{#if author}}
                <section class="author">
                    {{#if author.image}}
                        <img class ="authorimage"src="{{author.image}}" alt="Author image">
                    {{/if}}
                    {{#if author.website}}
                        <h4>Written by <a href="{{author.website}}" target="blank">{{author.name}}</a></h4>
                    {{else}}
                        <h4>by {{author.name}}</h4>
                    {{/if}}
                </section>
            {{/if}}
            <!-- Fin de la sección con los datos del autor -->


            <!-- Sección con los enlaces para compartir -->
            <!-- 

            <section class="share">
                <h4>Share this post</h4>
                <a href="http://twitter.com/share?text={{title}}&url={{url absolute="true"}}"
                    onclick="window.open(this.href, 'twitter-share', 'width=550,height=235');return false;">Twitter</a> | 
                </a>
                <a href="https://www.facebook.com/sharer/sharer.php?u={{url absolute="true"}}"
                    onclick="window.open(this.href, 'facebook-share','width=580,height=296');return false;">Facebook</a> | 
                </a>
                <a href="https://plus.google.com/share?url={{url absolute="true"}}"
                   onclick="window.open(this.href, 'google-plus-share', 'width=490,height=530');return false;">Google+</a>
                </a>
            </section> 

            -->
            <!-- Fin de la sección con los enlaces para compartir -->
        </footer>

        {{/post}}

    </article>

</main>